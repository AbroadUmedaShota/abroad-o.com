# TOOLディレクトリへのクッションページ（ポータル）作成

TOOLディレクトリ内の各ツール（HTML変換、フォルダ作成支援など）へアクセスしやすくするためのポータルページを作成する。

## 目的
ユーザーが `/TOOL` にアクセスした際、現在提供されているツールの一覧が表示され、目的のツールへ簡単に遷移できるようにする。

## 作業内容
- [ ] 現在の `TOOL/index.html` を `TOOL/excel_to_html.html` にリネームする。
- [ ] 新たな `TOOL/index.html` をポータルページとして作成する。
    - 会社のホームページのスタイル（`style3.css`など）と整合性をとる。
    - 各ツール（トリプルHTML変換、Excel HTML変換、フォルダ作成支援）へのリンクを設置する。
- [ ] `convert.html` や `mkfolder.html` からポータルページへの戻りリンクを確認・修正する。

## 完了定義
- `/TOOL`（`TOOL/index.html`）にアクセスすると、ツール一覧が表示される。
- 各リンクから対応するツールへ遷移できる。
- 既存のツール機能が損なわれていない。
