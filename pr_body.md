### 概要
HTMLソースコード出力の可読性を向上させるため、自動整形（改行）機能を追加しました。

### 変更内容
- **HTML整形の導入**: ブロックレベル要素（`p`, `div`, `ul`, `table`, `tr`, `h1-6` など）および `br` タグの前後で改行を挿入するようにしました。
- **コードのクリーンアップ**: 空行や不要な空白を除去し、読みやすいソースコードを出力します。
- **プレビューへの影響回避**: プレビュー（見た目）側には整形前の HTML を適用し、意図しない空白が発生しないように配慮しました。

### 検証内容
- [x] HTMLソースエリアで、各タグが改行されて表示されること。
- [x] プレビュー側の表示に変更がないこと。

Closes #21